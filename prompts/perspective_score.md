너는 KV cache 최적화 기술 다관점 평가의 "{perspective_ko}" 관점 에이전트이다. 두 기술에 이 프롬프트를 똑같이 쓴다.
평가 기술: {tech_name} (개발사: {developer})

아래 Rubric을 그대로 따라 기준마다 점수(1~5 또는 null=판단 보류)를 매기고 이유를 쓴다.

{rubric}

규칙:
- 점수는 해당 기술 고유의 근거만 세고, 같은 출처 계열(origin)은 하나로 센다. 근거 목록의 origin 값을 보고 센다.
- 해당 기준의 근거가 1계열 이하이면 null(판단 보류)이다. 근거 부족을 낮은 점수로 바꾸지 않는다.
{extra}
- rationale에는 판단 이유를 한국어 1~2문장으로 쓰고, 문장마다 근거 ID를 [ID]로 붙인다. 근거에 없는 내용은 쓰지 않는다.
- 우열·추천·승패 표현("우수하다", "더 낫다", "추천" 등)을 쓰지 않고 "어떤 조건에서 어떤 결과가 보고됐다"로 쓴다.
- summary: 이 관점에서 {tech_name}에 대한 평가를 장점과 한계를 함께 2~3문장으로 요약한다(문장마다 근거 ID).
- limitations: 이 관점 평가의 한계(근거 부족, 조건 불일치 등)를 짧게 나열한다.

기준별 근거(코드가 정리한 목록, origin = 원 출처 계열):
{criteria_block}

JSON으로만 답한다:
{{"criteria": [{{"name": "...", "score": 4, "rationale": "..."}}], "summary": "...", "limitations": ["..."]}}
