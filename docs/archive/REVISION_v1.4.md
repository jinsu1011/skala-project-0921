# 설계 개정 v1.4: 3차 리뷰·과제 안내 반영 기록

리뷰 목록(P0 구현 실패 · P1 평가 논리 · P2 문서 정합성)과 과제 안내 보완 2건을 항목별로 검토했다. 사실 확인 결과 그대로 따르지 않은 항목은 사유를 적었다. 결정 근거는 `docs/DECISIONS.md` D32~D40.

## 1. 항목별 처리

| ID | 리뷰 항목 | 처리 | 설계서 위치 | 비고 |
|---|---|---|---|---|
| P0-1 | `final_check` 한도 소진 경로 | 반영 | D.4-6, D.4-7, 그림 2a | 노드 추가 없이 `final_check` 내부 후처리(D32) |
| P0-2 | `Send` → `Command(goto=[노드명])` | 반영 | 요약, D.1 보조 노드, D.4-5, 그림 2a·2b(c) | `retry_router` 구현 형태를 D.4에 코드로 수록(D33) |
| P0-3 | 그림 2a 가짜 노드 제거, 루프 닫기 | 반영 | 그림 2a, Mermaid 소스, 캡션 | 점선은 관점 노드를 묶은 Fan-out 박스로 연결(개별 4선은 레이아웃이 엉켜 가독성 저하). 개념도임을 캡션에 명시 |
| P0-4 | 최악 superstep·`recursion_limit` | **수정 반영** | D.4-7, `pyproject.toml` | 리뷰의 "기본값 25"는 설치 버전(1.2.11) 기준 사실과 다름(실제 10,007). 50 명시 이유를 "상한 역할"로 고쳐 적음(D34) |
| P1-1 | H1 3×3 격자 | **수정 반영** | C.5, C.7 H1 | 리뷰안의 "해당 없음" 칸은 방향에 따라 대우가 비대칭 → 대각선 거리 기준(일치·부분 괴리·괴리)으로 대칭화(D36) |
| P1-2 | 관점별 결정적 검사, TRL 찬반 제외 | 반영 | D.5, JudgeScore | `bound_origins` 추가 |
| P1-3 | 개발사 발언 제외 | 반영 | C.3, C.5, C.8, D.6 | |
| P1-4 | 경쟁 진영 서술 주체 명시 | **보강 반영** | A.5 | TurboQuant p.18 서술과 KIVI p.5 원문 서술(잔차만 원정밀도)을 함께 기재(D37) |
| P1-5 | synthesis 판정 코드화 | 반영 | D.1, D.2, D.4-5, D.5 | |
| P1-6 | 재채점 동결, 검색 라운드 상한 | 반영 | D.4-5 | 9회 = 3라운드(초기 1 + 재질의 2) × 3회 실행 |
| P1-7 | 내부 Loop 표현 | (b) 선택 | D.1, 그림 2b 캡션 | 노드 18개·State 계약 유지(D35) |
| P2-1 | 3중 vs 2중 | (b) 선택 | B.6 결정 문단, 남은 한계 | 3중은 reranker 이전 v1 측정으로 먼저 고정. 동률 확인 후 교체는 사후 선택(D29·D39) |
| P2-2 | reranker 지연 병기 | **수정 반영** | B.6, D.6 | 리뷰안 "4.7(v1)/4.5(v2)"는 버전이 뒤바뀜. 실제: v1.0 첫 측정 4.7초, v1.2 재실행 4.5초, v2 4.7초 → "4.5~4.7초"와 회차별 값 공개. v1 CSV 지연 열이 v1.2 재실행 때 덮어쓰인 사실도 공개(D39) |
| P2-3 | 사전 적합성 단위 | 반영 | 요약, B.6 ① | 판정 결과 불변 |
| P2-4 | State 보완 | 반영 | D.2 | 카운터 초기값, `retrieval_grade` 설명, evidence 정렬·WAL |
| P2-5 | 갱신 안 된 문구 | 부분 반영 | D.6, 그림 2a | "도메인 50%" → "원 출처 계열 50%", 라우팅 노드 사각형. **Mermaid 소스는 D.3 본문에 유지**(Notion 요구 "Graph 흐름 설계(mermaid)", 부록 없음 구조 D31). 파일명은 이미 Notion 형식 |
| P2-6 | 변경 전후 요약표 | 반영 | "개정 요약" 절(요약 다음) | 변경 전 값에 도입 버전 표기, 남은 한계 목록 포함 |
| 안내 1 | 도메인 1개로 한정 | 반영 | 요약, A.4, B.2, C.1, C.3, C.5, C.7 H3, D.1, D.2, E.1 | 온디바이스 대조군 삭제, H3를 W1·W2 비교로 전환(D40) |
| 안내 2 | 평가 기준 = 에이전트용 Rubric | 반영 | C.6(신설) | 점수 앵커, 판단 보류, 기준별 긍정·부정 신호, 사람 표본 점검(`docs/RUBRIC_CHECK.md`) |

절 번호 변경: C.6 채점 Rubric 신설로 검증 가설은 C.7, 확증편향 방지 장치는 C.8이 되었다.

## 2. 수정 후 자체 점검 (자동 대조 포함)

| 점검 | 결과 |
|---|---|
| 모든 루프에 "통과"와 "한도 소진" 두 출구 | 통과. 공통 검색(소진 시 경고 후 진행), 관점 내부(소진 시 근거 부족), 관점 재실행(소진 시 판정 불확실), 보고서 검수(소진 시 후처리) |
| "Send로 재실행" 표현 없음 | 통과. 남은 `Send`는 개정 요약의 이력 행과 "Send를 쓰지 않는 이유" 설명뿐 |
| 그림 2a 상자가 실제 노드이거나 개념 상자임을 캡션에 명시 | 통과. Human 선정·공통 RAG 묶음 상자를 캡션에 명시 |
| 최악 superstep 수와 `recursion_limit` | 통과. 약 30, 50 명시, LangGraph 1.2.x 고정 |
| H1이 가설 두 절을 모두 판정 | 통과. 기대 선행 / 채택 신호 지연 양방향 |
| TRL에 찬반 할당량 미적용 | 통과. `bound_origins`로 대체 |
| 개발사 발언의 인식 점수 제외 | 통과 |
| 설명 없이 바뀐 수치 없음 | 통과. reranker 지연 회차별 공개 |
| 노드 18·State 27이 요약·표·그림과 일치 | 통과. 자동 대조: 노드명 18개 모두 본문 존재, State 표 27행 |
| 변경 전후 요약표와 남은 한계 | 통과 |
| 우열 어휘 | 통과. 사전 정의 문장(C.2 "우열 표현 교정") 외 0건 |
